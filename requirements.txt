# Optional. The agent runs, and scores the same 0.945297 on the public set,
# with none of this installed -- both features that use it degrade to the
# standard-library path and say so once on stderr.
#
# What it enables:
#   * semantic evidence (P7, E11, ON by default when available) -- scores a
#     paraphrased disclosure against a candidate's field values by meaning.
#     Inert on the public benchmark by construction; worth +0.1517 and
#     HitRate 0.805 -> 0.975 on a paraphrased replay of it.
#   * the dense retrieval route (P3, OFF by default) and the offline
#     comparison scripts under scripts/.
#
# Installing this pulls in torch, transformers, numpy and huggingface_hub
# transitively, and roughly doubles peak RSS (0.78 GB -> 1.60 GB). Set
# SHOPPING_AGENT_SEMANTIC=0 to keep the dependency installed but unused.
#
# No network at inference time and no API key at any time: voyage-4-nano is
# Apache-2.0 open weights and the catalogue vectors are prebuilt into
# data/embeddings/. Only the first run downloads model weights from the
# Hugging Face Hub; every run afterwards is fully offline.
sentence-transformers==6.0.0
