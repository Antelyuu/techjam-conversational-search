__pycache__/
*.py[cod]
.DS_Store
.env
results.json
# Ablation outputs are regenerable by their scripts. The figures that matter are
# transcribed into docs/experiments/.
fusion_ablation.json
clarification_ablation.json
rerank_ablation.json
rerank_weight_sweep.json
# The catalogue is distributed through the organiser's participant-kit release and
# is fetched at setup time; see docs/SETUP.md.
data/catalog.jsonl
data/releases/
# The MiniLM artifact is bundled so the optional dense route runs in a fresh
# runtime. bge-small is excluded: that model line was discontinued by team
# decision (E1) and its artifact is 73 MB.
data/embeddings/bge-small-en-v1.5.*
# Value-level embedding artifacts (E11): one vector per distinct card value,
# 268,564 of them, so 393 MB at MiniLM's 384 dimensions and more at Voyage's
# 2048. Research artifacts, rebuildable from scripts/build_value_embeddings.py.
# Whether any shippable form of these is committed is a separate decision --
# only a 256-dimension int8 build fits GitHub's 100 MB per-file limit.
data/embeddings/*-values.npy
data/embeddings/*-values.values.json
SHA256SUMS
# Organizer-only code, private evaluation data, manifests, and build reports.
organizer/
secure/
# Internal provenance and release-audit working documents.
docs/audits/
docs/data_selection_audit.md
docs/participant_release_checklist.md
# Organizer-only pipeline tests.
tests/test_5core_builder.py
tests/test_organizer_pipeline.py
# Local development artifacts and per-machine tooling state, none of which forms
# part of the submission.
*.log
.claude/
CLAUDE.md
PROGRESS.md
handoffs/
route_weight_sweep.json
tmpwork/
replay_trace.json
diagnostics/
# The Devpost write-up is submitted through the Devpost form and kept local by
# team decision. The README carries the same material for anyone reading the
# repository.
DEVPOST.md
# Internal team notes, maintained outside the submission.
explanation.md
